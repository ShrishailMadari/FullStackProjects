
spring:
  application:
    name: Product-Service
  datasource:
    url: jdbc:mysql://localhost:3306/FullStackApp

    username: root
    password: pass123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect